# Reale Konfiguration: enthält fahrzeugspezifische Daten und wird nicht
# versioniert. Nur die *.example.yaml gehören ins Repository.
config/hardware/*.yaml
!config/hardware/*.example.yaml
config/vehicle/*.yaml
!config/vehicle/*.example.yaml
# Ausnahme: die Demonstrationskonfiguration. Sie beschreibt ausdrücklich NICHT
# das reale Fahrzeug und enthält keine Adressen, Kapazitäten oder Schwellen.
# Sie gehört ins Repository, weil das System sonst ohne Wohnmobil nicht
# startbar wäre.
!config/vehicle/vehicle.simulation.yaml
config/environments/*.local.yaml

# Secrets gehören niemals ins Repository (Kapitel 15 §49/§50)
.env
.env.*
!.env.example
*.key
*.pem

# Daten und Laufzeit
*.db
*.db-wal
*.db-shm
data/
logs/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Gebaute Oberfläche. Ein Bauartefakt gehört nicht in die Versionsverwaltung —
# es entsteht reproduzierbar aus den Quellen ('npm run build'). Fehlt es,
# startet das Backend trotzdem und weist im Log darauf hin.
backend/kehleros/api/static/

# System
.DS_Store
